# 高斯光斑模拟模块 - 功能说明文档

## 📋 模块概述

**高斯光斑模拟模块**是为激光原理课程设计项目新增的第五阶段功能。该模块使用 HTML5 Canvas 技术，根据高斯分布公式实时绘制二维高斯光斑，提供动态、交互式的光学可视化体验。

---

## 🎯 核心功能

### 1. **实时二维光斑绘制**
- 基于高斯分布公式：**I(r) = I₀ × exp(-2r²/w²)**
- 中心最亮（纯白色），边缘逐渐变暗并透明
- 光斑轮廓圆（w₀ 束腰处）清晰标注
- 网格参考线帮助理解光斑大小

### 2. **参数实时响应**
- 修改左侧参数输入框时，光斑立即更新
- 支持的参数：
  - 波长 λ (nm)：影响瑞利长度
  - 束腰 w₀ (mm)：直接影响光斑大小
  - 传播距离 z (m)：影响束腰处的光强比
  
### 3. **动态参数面板**
- 实时显示当前输入参数
- 显示传播距离处的实时光强比
- 中心光强指示（I₀ = 1.0）
- 公式和物理意义说明

### 4. **高清显示支持**
- 自动适应设备像素比（DPI）
- 清晰显示各种屏幕分辨率
- Canvas 大小自动调整

### 5. **交互式动画**
- 使用 requestAnimationFrame 实现平滑动画
- 参数变化时无缝更新
- 窗口尺寸变化时自动响应

---

## 📁 文件结构

```
项目根目录/
├── index.html          # HTML 结构（已修改，添加光斑容器）
├── style.css           # 主样式（已修改，优化布局）
├── script.js           # 计算模块（未修改）
├── animation.js        # 动画模块（未修改）
├── spot.css            # 光斑模块样式（新增）
├── spot.js             # 光斑模块功能（新增）
└── SPOT_MODULE.md      # 本文档
```

---

## 🔧 技术细节

### Canvas 绘图技术
```javascript
// 核心公式实现
I(r) = I₀ × exp(-2r²/w²)

// 高斯分布计算
intensity = centerIntensity * Math.exp(-2 * normalizedRadius²)
```

### 径向渐变实现
光斑使用 Canvas 的 `createRadialGradient` 实现平滑过渡：
```
中心：纯白色 (RGBA 255,255,255,1)
    ↓
内圆：亮白色 (RGBA 255,255,255,0.9)
    ↓
中层：亮青绿 (RGBA 100,255,240,0.8)
    ↓
外层：中等青绿 (RGBA 66,215,200,0.5)
    ↓
边缘：淡青绿 (RGBA 66,215,200,0.2)
    ↓
最边缘：透明 (RGBA 66,215,200,0)
```

### 性能优化
- 使用单个 Canvas 元素避免重复 DOM 操作
- 利用 requestAnimationFrame 实现浏览器级别同步
- 设备像素比自适应，确保高清显示
- 参数有效性检查，避免无效绘制

---

## 🎨 页面布局变化

### 修改前
```
┌─────────────────────────────────────────────┐
│  页面顶部
├────────────┬────────────────┬────────────────┤
│ 参数控制   │ 光束传播显示   │  计算结果       │
├────────────┼────────────────┼────────────────┤
```

### 修改后
```
┌─────────────────────────────────────────────┐
│  页面顶部
├─────────────────────────────────────────────┤
│  高斯光斑模拟模块（新增）                   │
├────────────┬────────────────┬────────────────┤
│ 参数控制   │ 光束传播显示   │  计算结果       │
├────────────┼────────────────┼────────────────┤
```

---

## 🎬 动画与实时更新机制

### 帧更新流程
```
requestAnimationFrame 触发
    ↓
读取输入框参数
    ↓
单位转换（nm→m, mm→m）
    ↓
计算光束参数（瑞利长度、束腰半径等）
    ↓
更新参数显示面板
    ↓
绘制背景
    ↓
绘制高斯光斑（使用 Canvas 径向渐变）
    ↓
绘制参考圆和中心标记
    ↓
绘制网格和参数标注
    ↓
下一帧...
```

### 事件绑定
- **input 事件**：用户输入时实时更新
- **change 事件**：输入框失焦时更新
- **resize 事件**：窗口大小变化时调整 Canvas

---

## 💡 物理含义

### 高斯分布公式详解
```
I(r) = I₀ × exp(-2r²/w²)

其中：
- I(r)：距离光轴 r 处的光强
- I₀：中心光强（最大值）
- w：光束半径（1/e² 处）
- r：距光轴的径向距离

当 r = w 时：I(w) = I₀ × e^(-2) ≈ 0.135I₀
即光强下降到中心的 13.5%
```

### 束腰与光斑大小的关系
- 束腰越小 → 光斑越小、越亮
- 束腰越大 → 光斑越大、越暗
- 传播距离影响实时光强比

---

## 🖥️ 使用说明

### 基本操作
1. **打开页面** - 页面顶部会显示光斑模块
2. **输入参数** - 在左侧参数控制区输入或修改值
3. **观察光斑** - 光斑会实时响应参数变化
4. **查看数据** - 光斑下方显示实时参数信息

### 推荐参数组合
```
λ = 632.8 nm     (He-Ne激光)
w₀ = 0.5 mm      (束腰)
z = 1.0 m        (传播距离)
```

### 参数范围
- 波长：100~10000 nm（可见光到红外）
- 束腰：0.1~10 mm
- 传播距离：0~100 m

---

## 🔍 代码组织结构

### spot.js 的模块化设计

#### 第一部分：DOM 元素获取和初始化
获取页面元素引用，初始化全局变量

#### 第二部分：HTML 结构创建
动态生成光斑容器的完整 HTML

#### 第三部分：单位转换函数
处理 nm、mm、m 之间的转换

#### 第四部分：参数读取和更新
从输入框读取参数并进行有效性检查

#### 第五部分：Canvas 尺寸管理
适配容器大小和设备像素比

#### 第六部分：核心绘图函数
实现高斯分布计算和 Canvas 绘制

#### 第七部分：主要绘制函数
组织绘制逻辑的入口函数

#### 第八部分：事件绑定和交互
处理用户交互事件

#### 第九部分：模块初始化
统筹初始化流程

#### 第十部分：模块启动
启动整个模块并处理清理

---

## ⚙️ API 接口

### 外部可调用的属性
```javascript
window.gaussianSpotModuleStatus
// 状态值：'loading' | 'ready' | 'failed'
```

### 内部关键函数

| 函数名 | 功能 |
|--------|------|
| `initializeSpotContainer()` | 创建 HTML 结构 |
| `readSpotParameters()` | 读取参数 |
| `resizeSpotCanvas()` | 调整 Canvas 大小 |
| `drawGaussianSpot()` | 绘制光斑 |
| `renderGaussianSpot()` | 完整渲染流程 |
| `animateGaussianSpot()` | 动画循环 |

---

## 🐛 故障排除

### 光斑不显示
- ✅ 检查浏览器控制台是否有错误
- ✅ 确认 spot.js 和 spot.css 已正确引入
- ✅ 检查参数是否有效（大于 0）

### 光斑显示模糊
- ✅ 检查设备像素比设置
- ✅ 尝试刷新页面
- ✅ 检查浏览器缩放级别（应为 100%）

### 光斑更新不及时
- ✅ 检查浏览器是否支持 requestAnimationFrame
- ✅ 检查参数输入是否正确
- ✅ 查看浏览器开发者工具的性能选项卡

### 参数不更新
- ✅ 确认左侧输入框有焦点
- ✅ 检查数值是否在有效范围内
- ✅ 尝试点击输入框后手动输入

---

## 📚 参考资源

### 相关物理知识
- **高斯光束**：具有高斯分布光强的激光束
- **瑞利长度**：zR = πw₀²/λ
- **光束半径**：w(z) = w₀√(1 + (z/zR)²)
- **发散角**：θ ≈ λ/(πw₀)

### Web 技术参考
- HTML5 Canvas API
- requestAnimationFrame
- CSS3 Grid 布局
- 响应式网页设计

---

## 📝 更新日志

### v1.0 (当前版本)
- ✅ 实现基础高斯光斑绘制
- ✅ 添加参数实时更新机制
- ✅ 实现 Canvas 响应式设计
- ✅ 添加参数显示面板
- ✅ 支持多屏适配

### 未来计划
- ⏳ 3D 光斑旋转视图
- ⏳ 光强分布图表
- ⏳ 光斑功率计算
- ⏳ 导出光斑图像

---

## 📧 技术支持

如有问题或建议，请检查：
1. 浏览器控制台错误信息
2. 网络连接状态
3. 外部资源加载状态
4. 浏览器兼容性

---

**最后更新时间：** 2026-05-30  
**兼容浏览器：** Chrome 60+, Firefox 55+, Safari 12+, Edge 79+  
**模块状态：** ✅ 生产就绪
